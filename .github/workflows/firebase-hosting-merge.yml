# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
env:
  firebaseConfigApiKey: ${{ secrets.FIREBASECONFIGAPIKEY }}
  firebaseConfigAuthDomain: ${{ secrets.FIREBASECONFIGAUTHDOMAIN }}
  firebaseConfigProjectId: ${{ secrets.FIREBASECONFIGPROJECTID }}
  firebaseConfigStorageBucket: ${{ secrets.FIREBASECONFIGSTORAGEBUCKET }}
  firebaseConfigMessagingSenderId: ${{ secrets.FIREBASECONFIGMESSAGINGSENDERID }}
  firebaseConfigAppId: ${{ secrets.FIREBASECONFIGAPPID }}
  firebaseConfigMeasurementId: ${{ secrets.FIREBASECONFIGMEASUREMENTID }}
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    environment: production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TOMAT_D4561 }}'
          channelId: live
          projectId: tomat-d4561
