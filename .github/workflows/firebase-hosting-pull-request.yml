# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR'
env:
  VITE_FIREBASECONFIG_APIKEY: ${{ secrets.VITE_FIREBASECONFIG_APIKEY }}
  VITE_FIREBASECONFIG_AUTHDOMAIN: ${{ secrets.VITE_FIREBASECONFIG_AUTHDOMAIN }}
  VITE_FIREBASECONFIG_PROJECTID: ${{ secrets.VITE_FIREBASECONFIG_PROJECTID }}
  VITE_FIREBASECONFIG_STORAGEBUCKET: ${{ secrets.VITE_FIREBASECONFIG_STORAGEBUCKET }}
  VITE_FIREBASECONFIG_MESSAGINGSENDERID: ${{ secrets.VITE_FIREBASECONFIG_MESSAGINGSENDERID }}
  VITE_FIREBASECONFIG_APPID: ${{ secrets.VITE_FIREBASECONFIG_APPID }}
  VITE_FIREBASECONFIG_MEASUREMENTID: ${{ secrets.VITE_FIREBASECONFIG_MEASUREMENTID }}
'on': pull_request
jobs:
  build_and_preview:
    environment: production
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TOMAT_D4561 }}'
          projectId: tomat-d4561
